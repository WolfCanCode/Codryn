# codryn ui

React + TypeScript + Vite frontend for the `codryn` dashboard.

## Scripts

```bash
npm install
npm run dev
npm run build
```

## Notes

- built output goes to `ui/dist`
- the Rust crate at `crates/codryn-ui` embeds `ui/dist`
- this frontend powers the Projects, Analytics, Graph, and Config pages
